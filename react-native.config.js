module.exports = {
  // react-native-vector-icons fonts are handled by CocoaPods (RNVectorIcons pod)
  // on iOS, so we do NOT list them here to avoid duplicate Copy Bundle Resources.
  assets: [],
};
